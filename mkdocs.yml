# mkdocs.yml
# Configuration file for MkDocs (Automated project documentation generator)

site_name: datakit-lite
site_url: https://denisecase.github.io/datakit-lite/
repo_url: https://github.com/denisecase/datakit-lite
site_description: Helpful utilities for Python data projects.
site_author: denisecase
docs_dir: docs
site_dir: site
copyright: Copyright &copy; 2025 Denise Case
extra:
  social:
    - icon: fontawesome/brands/github
      link: https://github.com/denisecase/datakit-lite
  version:
    provider: mike

theme:
  name: material
  features:
    - navigation.expand
    - navigation.sections # Group navigation items
    - navigation.top # Back-to-top button
    - search.suggest # Search suggestions as you type
    - search.highlight # Highlight search terms
    - content.code.copy # Copy button on code blocks
    - toc.follow # Highlight current section in TOC

  palette:
    - scheme: default # Light/dark mode toggle
      primary: blue
      accent: blue
      toggle:
        icon: material/brightness-7
        name: Switch to dark mode
    - scheme: slate
      primary: blue
      accent: blue
      toggle:
        icon: material/brightness-4
        name: Switch to light mode

plugins:
  - search
  - mike:
      version_selector: true
      css_dir: css
      javascript_dir: js
      canonical_version: latest
  - mkdocstrings:
      handlers:
        python:
          paths: [src]
          options:
            show_source: true
            show_root_heading: true # Include module names
            show_root_toc_entry: true # Include module in TOC
            docstring_style: google # Professional docstring format
            show_signature_annotations: true
            separate_signature: true    # Makes function signatures easier to read

markdown_extensions:
  - admonition # Note/Warning/Tip boxes
  - codehilite # Syntax highlighting
  - tables # Markdown tables
  - toc:
      permalink: true # Clickable headers
      toc_depth: 3
  - pymdownx.superfences # Code blocks with syntax highlighting
  - pymdownx.details # Collapsible sections
  - pymdownx.inlinehilite # Inline code highlighting
  - pymdownx.snippets # Include external files
  - pymdownx.tabbed # Tabbed content

nav:
  - Home: index.md
  - API Reference: api.md
  - CLI Reference: cli.md
